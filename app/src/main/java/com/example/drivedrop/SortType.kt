package com.example.drivedrop

//Types to be able to sort
// part of the old database testing (first increment) to check on filter option
enum class SortType {
    FIRST_NAME,
    EMAIL,
    USER_NAME,
}